'use client';

import React from 'react';

export interface Card3DMeshProps {
  presetId?: string;
  intensity?: number;
  frontTextureUrl?: string;
  backTextureUrl?: string;
  isHovered?: boolean;
}

export function Card3DMesh(props: Card3DMeshProps) {
  return null;
}
